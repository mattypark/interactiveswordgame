# Copy to .env.local and paste your own values. Never commit the real file.

# Convex deployment URL for live versus. Run `npx convex dev` once and it will
# print this (and write it into .env.local for you).
# Without it, Fight and Sandbox work normally and Versus says it isn't set up.
VITE_CONVEX_URL=
